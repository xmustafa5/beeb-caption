# Beeb backend base URL (public staging). Override per-environment as needed.
EXPO_PUBLIC_API_URL=https://beeb.madebyhaithem.com

# OSRM routing server used for drawing route polylines (public demo server — no SLA).
EXPO_PUBLIC_OSRM_URL=https://router.project-osrm.org
